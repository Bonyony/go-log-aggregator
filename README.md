# Go Log Aggregator

This is a log aggregator built with Go designed to generate a fixed set of log files -> send these to Google Cloud's Pub/Sub -> pull this data from Pub/Sub and aggregate the logs into readable text -> (possibly) display this data on a web page in real time, but this project is more about speed and performance over visual data...

:)
